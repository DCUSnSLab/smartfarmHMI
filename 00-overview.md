# 00 · 시스템 개요

## 1. 사업 배경

RISE 피지컬 AI 기반 스마트농업 생태계 구축 사업 2차년도(2026.05~2027.01)는 6개 R&D 프로그램으로 구성된다.

| 프로그램 | 내용 |
|---|---|
| ① 농작업용 그리퍼 개발 | 대상 작물: 참외 |
| ② 피지컬 AI 학습 시스템 구축 | |
| ③ 식물 공장 및 자동화 시스템 개발 | |
| ④ 스마트 온실 구축 | 50~100평 |
| ⑤ 음성 인식 시스템 개발 | |
| ⑥ 웹앱 기반 스마트팜 HMI 개발 | 본 문서의 대상 |

이 중 **⑥ 웹앱 기반 스마트팜 HMI 개발**을 다룬다. 이름은 "웹앱 개발"이지만, 실제 범위는 프론트엔드(웹앱) + **애플리케이션 서버 + 미들웨어 서버** + 통신 규격을 포함한다 (서버가 1개가 아니라 역할이 다른 2개로 나뉜다 — 상세는 `03-architecture/system-architecture.md`).

## 2. 개발 범위

### In Scope
프론트엔드(웹앱) + 애플리케이션 서버 + 미들웨어 서버 + 각 구간(웹앱-애플리케이션 서버, 애플리케이션 서버-미들웨어 서버, 미들웨어 서버-엣지) 통신 규격. 세부 기능은 `01-requirements/functional.md` 참고.

### Out of Scope
- 실물 하드웨어 (로봇, 생육기, 센서 등)
- 엣지 자체 개발 (엣지 컨트롤러 펌웨어·드라이버)
- 로봇 제어·경로계획 알고리즘
- AI 모델 자체 (VLM/LLM/Perception 모델 학습)
- 음성 인식 엔진
- 경북도 방언 수집
- 웹앱 기반 로봇 원격조종기 (로봇팔 직접 조작)
- 로봇/스마트팜 증강현실 교육시스템
- 스마트팜 디지털트윈
- 로봇 원격교육시스템
- 수확 작업 (FR-01 중 수확 관련 로깅 포함, 3차년도 진행)

### 전제
- 엣지는 데이터를 송수신하는 상대로 가정한다. 실물 엣지가 없는 2차년도 기간에는 임의 데이터 생성기로 대체한다.
- 데이터 타입·크기·주기는 확정되어 있지 않고 변경 가능성이 크므로, 특정 규격에 고정하지 않고 확장 가능한 구조로 구현한다.
- 양액·급수·방재는 파이프(관) 배관이 아니다. 로봇이 재배 트레이(파레트)를 랙에서 반출해 작업 장소로 옮기고, 작업 후 원래 슬롯에 반납하는 모듈형 방식이다.

## 3. 시스템 구축 목표

1. 모니터링 — 생육환경(온·습도·양분·조도·전력), 탱크 수위, 로봇 상태(위치·속도·전원·이상)를 웹앱에서 실시간 확인
2. 제어 — 생육기 환경(온·습도·조도)과 로봇 임무(트레이 반출·양액·급수·방재·스케줄링·충전)를 웹앱에서 수동/자동 제어
3. 데이터 관리 — 수집 데이터 통계·조회, 농업일지 자동 생성, 관리자 메모
4. 음성/LLM 인터페이스 — 음성 명령·대화형 도움말을 위한 UI와 외부 엔진 연동 훅만 구현한다. 음성 인식 엔진과 LLM 자체는 개발 범위 밖이며, 2차년도 내 실제 연동이 되지 않을 가능성이 높다. 미연동 시 텍스트 입력/정적 안내로 동작해야 한다.
5. 원격 접근 — 웹/앱 기반 원격 인증·라우팅

2차년도 데모 목표, 일정(8월 실 하드웨어 연동)과 현재 개발 범위는 `04-roadmap/phase2-scope.md` 참고.

엣지 실물이 없는 기간에는 임의 데이터 생성기가 엣지 역할을 대신하며, 미들웨어 서버가 이를 수집·저장하고, 애플리케이션 서버·웹앱이 조회하는 구조로 개발을 진행한다. 미들웨어 서버의 엣지 통신 부분은 인터페이스로 분리해, 이후 실제 엣지 구현체로 교체할 수 있도록 설계한다. 시스템 구성과 다이어그램은 `03-architecture/system-architecture.md`, 기능별 컴포넌트 책임은 `03-architecture/component-mapping.md`, 각 컴포넌트 내부 구성은 `03-architecture/component-internals.md`, 통신 규격은 `03-architecture/communication-interface.md`에서 다룬다.

## 변경 이력
- 2026-07-07 · 최초 작성
