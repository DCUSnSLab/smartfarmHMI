# 00 · 시스템 개요

## 1. 사업 배경

RISE 피지컬 AI 기반 스마트농업 생태계 구축 사업 2차년도(2026.05~2027.01)는 6개 R&D 프로그램으로 구성된다.

| 프로그램 | 내용 |
|---|---|
| ① 농작업용 그리퍼 개발 | 대상 작물: 참외 |
| ② 피지컬 AI 학습 시스템 구축 | |
| ③ 식물 공장 및 자동화 시스템 개발 | |
| ④ 스마트 온실 구축 | 50~100평 |
| ⑤ 음성 인식 시스템 개발 | |
| ⑥ 웹앱 기반 스마트팜 HMI 개발 | 본 문서의 대상 |

이 중 **⑥ 웹앱 기반 스마트팜 HMI 개발**을 다룬다. 이름은 "웹앱 개발"이지만, 실제 범위는 프론트엔드(웹앱) + **애플리케이션 서버 + 미들웨어 서버** + 통신 규격을 포함한다 (서버가 1개가 아니라 역할이 다른 2개로 나뉜다 — 상세는 `docs/03-architecture/system-architecture.md`).

## 2. 개발 범위

### In Scope
프론트엔드(웹앱) + 애플리케이션 서버 + 미들웨어 서버 + 각 구간(웹앱-애플리케이션 서버, 애플리케이션 서버-미들웨어 서버, 미들웨어 서버-엣지) 통신 규격. 세부 기능은 `docs/01-requirements/functional.md` 참고.

다음 항목은 요구사항 정합화 과정에서 범위 내로 확정한 것이다.

- **다농장(복수 스마트팜) 구조** — 아래 "다농장 전제" 참고
- **알림 도메인** — 임계 초과·장비 이상·통신 단절·작업 완료 알림의 생성·조회·규칙 설정 (FR-32~34)
- **원격 전체 정지와 물리 비상정지 상태 표시** — 아래 "안전 관련 범위 구분" 참고 (FR-35·36)
- **장치 통신 상태 감시** — MQTT LWT/birth-death 기반 오프라인 감지 (FR-37)
- **외부 기상 정보 연동·표시** — 외부 기상 API를 조회해 농장별 지역 날씨를 표시한다 (FR-40). 공급자는 미정 (OPN-17)

### Out of Scope
- 실물 하드웨어 (로봇, 생육기, 센서 등)
- 엣지 자체 개발 (엣지 컨트롤러 펌웨어·드라이버)
- 로봇 제어·경로계획 알고리즘
- AI 모델 자체 (VLM/LLM/Perception 모델 학습)
- 음성 인식 엔진
- 경북도 방언 수집
- 웹앱 기반 로봇 원격조종기 (로봇팔 직접 조작)
- 로봇/스마트팜 증강현실 교육시스템
- 스마트팜 디지털트윈
- 로봇 원격교육시스템
- 수확 작업 (FR-01 중 수확 관련 로깅 포함, 3차년도 진행)
- **환기·차광·천창 액추에이터 제어** — 외기 조건에 따른 차광·환기 권고는 참고 정보로 표시할 수 있으나, 해당 설비를 제어하지는 않는다
- **물리 비상정지 장치 자체** — 아래 "안전 관련 범위 구분" 참고

### 전제
- 엣지는 데이터를 송수신하는 상대로 가정한다. 실물 엣지가 없는 2차년도 기간에는 임의 데이터 생성기로 대체한다.
- 데이터 타입·크기·주기는 확정되어 있지 않고 변경 가능성이 크므로, 특정 규격에 고정하지 않고 확장 가능한 구조로 구현한다.
- 양액·급수·방재는 파이프(관) 배관이 아니다. 로봇이 재배 트레이(파레트)를 랙에서 반출해 작업 장소로 옮기고, 작업 후 원래 슬롯에 반납하는 모듈형 방식이다.

### 다농장 전제

**설계·구현은 농장 N개를 전제로 하고, 2차년도 실운영은 농장 1개로 진행한다(테스트).** 웹앱 UI도 다농장 기준으로 설계한다 (전체 현황 ↔ 특정 농장 전환, FR-38).

농장이 1개뿐인 기간에도 다농장 구조를 그대로 쓴다. 데이터 모델의 모든 엔터티는 농장 식별자를 갖고(`docs/02-domain/data-model.md`), MQTT 토픽도 농장 단위로 계층화한다(`docs/03-architecture/communication-interface.md`). 나중에 농장을 추가할 때 스키마·토픽 구조를 바꾸지 않아도 되도록 하는 것이 목적이다. 실운영 전환 시점은 미정 (OPN-23).

### 안전 관련 범위 구분

정지 기능은 성격이 다른 두 가지로 나뉜다. 표준상 요구사항이 전혀 다르므로 문서 전반에서 구분해 기술한다.

| 구분 | 성격 | 담당 | 웹앱의 역할 |
|---|---|---|---|
| **물리 비상정지** (ISO 13850) | 안전 기능. 물리적 직접개로 + 기계식 래치 장치. IEC 60204-1 Stop Category 0/1 | 엣지·하드웨어팀 (**범위 밖**) | **상태 표시 전용.** 해제는 현장에서 물리 조작으로만 가능하며 웹앱에 해제 경로를 두지 않는다 (FR-36) |
| **원격 전체 정지** | 운전 정지. IEC 60204-1 Stop Category 2 (제어된 정지, 전원 유지). **비안전등급** | 우리 팀 (범위 내) | 발동·해제 모두 웹앱에서 수행. 발동 시 자동 스케줄·원격 제어를 차단한다 (FR-35) |

ISO 13850은 터치스크린이나 화면상의 그래픽 버튼을 비상정지 장치로 인정하지 않는다. 따라서 웹앱이 제공하는 정지 기능은 "비상정지"가 아니라 "원격 전체 정지"이며, 문서·UI 어디에서도 이를 비상정지로 표기하지 않는다. 근거와 페일세이프 설계는 `docs/01-requirements/non-functional.md` 참고.

## 3. 시스템 구축 목표

1. 모니터링 — 생육환경(온·습도·양분·조도·전력), 탱크 수위, 로봇 상태(위치·속도·전원·이상)를 웹앱에서 실시간 확인
2. 제어 — 생육기 환경(온·습도·조도)과 로봇 임무(트레이 반출·양액·급수·방재·스케줄링·충전)를 웹앱에서 수동/자동 제어
3. 데이터 관리 — 수집 데이터 통계·조회, 농업일지 자동 생성, 관리자 메모
4. 음성/LLM 인터페이스 — 음성 명령·대화형 도움말을 위한 UI와 외부 엔진 연동 훅만 구현한다. 음성 인식 엔진과 LLM 자체는 개발 범위 밖이며, 2차년도 내 실제 연동이 되지 않을 가능성이 높다. 미연동 시 텍스트 입력/정적 안내로 동작해야 한다.
5. 원격 접근 — 웹/앱 기반 원격 인증·라우팅
6. 이상 감지·전달 — 임계 초과·장비 이상·통신 단절을 알림으로 전달하고, 정지 상태를 표시 (FR-32~37)

2차년도 데모 목표, 일정(8월 실 하드웨어 연동)과 현재 개발 범위는 `docs/04-roadmap/phase2-scope.md` 참고.

엣지 실물이 없는 기간에는 임의 데이터 생성기가 엣지 역할을 대신하며, 미들웨어 서버가 이를 수집·저장하고, 애플리케이션 서버·웹앱이 조회하는 구조로 개발을 진행한다. 엣지 구간은 MQTT로 확정했고, 임의 데이터 생성기도 실제 엣지와 동일한 토픽·스키마를 사용하므로 이후 실제 엣지 구현체로 교체할 수 있다. 시스템 구성과 다이어그램은 `docs/03-architecture/system-architecture.md`, 기능별 컴포넌트 책임은 `docs/03-architecture/component-mapping.md`, 각 컴포넌트 내부 구성은 `docs/03-architecture/component-internals.md`, 통신 규격은 `docs/03-architecture/communication-interface.md`에서 다룬다.

## 변경 이력
- 2026-07-07 · 최초 작성
- 2026-07-29 · 문서 이동에 따른 참조 경로 정리. 다농장 전제, 안전 관련 범위 구분, 알림·기상·통신 감시 In Scope 추가. 환기·차광·천창 제어와 물리 비상정지 장치를 Out of Scope로 명시
