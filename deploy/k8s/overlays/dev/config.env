# DEV 환경 ConfigMap 값 — Secret(비밀번호·키)은 여기 두지 않는다 (README 참고)
DJANGO_SETTINGS_MODULE=config.settings.prod
DJANGO_DEBUG=0
DJANGO_ALLOWED_HOSTS=*
POSTGRES_HOST=timescaledb
POSTGRES_DB=smartfarm
REDIS_URL=redis://redis:6379/0
MQTT_HOST=mosquitto
MQTT_PORT=1883
