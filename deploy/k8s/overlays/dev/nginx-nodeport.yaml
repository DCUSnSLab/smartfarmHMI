apiVersion: v1
kind: Service
metadata:
  name: smartfarmhmi-nginx
spec:
  # 고정 IP(203.250.33.77)는 운영(main)이 쓴다 — dev 는 NodePort 로 노출한다.
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30480
